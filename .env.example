# Solo lo usa el servidor alternativo de Node.js (server.js), que sincroniza
# tareas en tiempo real vía Firebase Firestore. El backend principal (PHP,
# api/*.php) toma sus credenciales de config/conexion.php, no de este archivo.

PORT=3000
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json
